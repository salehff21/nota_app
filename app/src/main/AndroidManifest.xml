<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nota_app">
    <!-- إذن جدولة التنبيهات بدقة -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions" /> <!-- إذن إرسال الإشعارات مطلوب من Android 13 (API 33) وما بعده -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- إذن استقبال إعادة تشغيل الجهاز لضمان جدولة التنبيهات بعد إعادة التشغيل -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- إذن تسجيل الصوت -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- أذونات التخزين الجديدة لـ Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/event_management"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nota_app"
        tools:targetApi="33">
        <activity
            android:name=".ReMYWActivity"
            android:exported="false" />
        <activity
            android:name=".ReportsActivity"
            android:exported="false" />
        <activity
            android:name=".ActivityAdd"
            android:exported="false" /> <!-- الشاشة الرئيسية للتطبيق -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- استقبال التنبيهات -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false">

            <!-- السماح لـ AlarmReceiver باستقبال التنبيه بعد إعادة تشغيل الجهاز -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>